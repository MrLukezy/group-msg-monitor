# QQ 群消息实时监控方案大纲

> 版本：v0.2  
> 日期：2026-07-27  
> 状态：已落地 MVP（监控小服务可运行；协议端需自行安装 NapCat）  
> 约束：**不能向目标群拉入机器人号**；使用本机已登录 / 可登录的个人 QQ 号接收消息

---

## 1. 目标与范围

### 1.1 目标

搭建一套运行在本机（或内网）的小型服务，**实时接收指定 QQ 群的消息**，并支持后续扩展：日志落盘、关键词告警、Webhook 转发等。

### 1.2 成功标准

| 编号 | 标准 | 说明 |
|------|------|------|
| S1 | 实时性 | 群内新消息通常在数秒内到达监控服务 |
| S2 | 可过滤 | 仅处理配置的 `group_id`，忽略其他会话 |
| S3 | 不进群加号 | 不依赖「官方机器人 / 额外号进群」 |
| S4 | 本机可控 | 接口默认仅本机可访问，带访问令牌 |
| S5 | 可扩展 | 后续可加告警、存储、转发，而不改协议层 |

### 1.3 非目标（本期不做）

- 不解密、不轮询本机 `nt_msg.db` 作为主数据源
- 不做完整聊天记录历史导出（可后续单独做）
- 不对外公网暴露 OneBot 接口
- 不实现复杂群管理（踢人、禁言等）——监控场景以**接收**为主

---

## 2. 背景与约束

### 2.1 现状

- 本机已安装并登录新版 QQ（NTQQ）
- 账号本地数据目录示例：`Documents\Tencent Files\<QQ号>\nt_qq\`
- 消息库（如 `nt_msg.db`）为加密 SQLite，**不适合作为实时监控主路径**

### 2.2 硬约束

1. **不能把机器人拉进目标群** → 排除「官方开放平台机器人进群」方案  
2. 监控账号必须**已经在群内**（个人号视角收消息）  
3. 优先稳定性与安全，接受社区协议方案的风控现实

### 2.3 关键澄清

「不能拉机器人进群」≠「不能用 NapCat / OneBot」。

NapCat / LLOneBot 是给**你自己已在群里的 QQ 号**加一层协议桥，把你本机收到的消息以标准事件推给小服务；**群成员列表不会因此多出一个机器人账号**。

---

## 3. 方案选型结论

### 3.1 推荐方案

**个人号协议桥（NapCat） + OneBot 事件总线 + 本地监控小服务**

| 层级 | 组件 | 职责 |
|------|------|------|
| L1 协议端 | NapCatQQ | 登录 QQ，将 NTQQ 消息转为 OneBot 11 事件 |
| L2 传输 | WebSocket（推荐）或 HTTP POST | 实时推送事件到本机 |
| L3 业务 | 自研小服务（Python 优先） | 过滤群、落库/告警/转发 |
| L4 配置 | `.env` / `config.yaml` | 群号、Token、告警开关等 |

### 3.2 备选方案

| 方案 | 何时选用 | 说明 |
|------|----------|------|
| LLOneBot | 必须继续使用带界面的正式 QQ 客户端 | 以插件/框架方式挂接现有 NTQQ |
| 官方 QQ 机器人 | 将来允许拉机器人进群 | 合规更好，但当前约束下不可用 |
| 读本地加密库 | 仅离线取证/导出 | 实时性差、解密成本高，不推荐主路径 |
| 桌面 OCR / UI 自动化 | 协议桥完全不可用时的权宜之计 | 脆弱、易漏消息，仅作兜底 |

### 3.3 不推荐

- 公网裸奔 OneBot（无 Token、`0.0.0.0` 暴露）
- 主力生活号长期高频发消息做 Bot 行为
- 自研完整 QQ 协议栈

---

## 4. 总体架构

```text
┌─────────────────────────────────────────────────────────┐
│  本机                                                    │
│                                                         │
│   ┌──────────────┐         OneBot 11          ┌───────┐ │
│   │   NapCat     │  WS / HTTP 事件推送         │ 监控   │ │
│   │  (协议端)     │ ─────────────────────────► │ 小服务 │ │
│   │  个人号登录   │  ◄───────────────────────── │       │ │
│   └──────────────┘   可选：主动 API（少用）     └───────┘ │
│          │                                       │      │
│          │ 收群消息                               ├─ 日志 │
│          ▼                                       ├─ SQLite│
│     目标 QQ 群                                   └─ Webhook│
└─────────────────────────────────────────────────────────┘
```

### 4.1 数据流（接收）

1. 群成员发言 → QQ 服务器 → NapCat 所登录账号收到消息  
2. NapCat 封装为 OneBot 事件（`post_type=message`, `message_type=group`）  
3. 经本机 WebSocket 推送到监控服务  
4. 服务校验 Token → 过滤 `group_id` → 处理（写日志 / 告警 / 转发）

### 4.2 事件示例（示意）

```json
{
  "post_type": "message",
  "message_type": "group",
  "group_id": 123456789,
  "user_id": 987654321,
  "sender": { "nickname": "张三", "card": "群名片" },
  "raw_message": "大家好",
  "message_id": "xxxxx",
  "time": 1720000000
}
```

---

## 5. 组件设计

### 5.1 协议端（NapCat）

**职责**

- 维持 QQ 登录会话
- 对外提供 OneBot 11 兼容接口
- 推送消息 / 通知类事件

**配置原则**

| 项 | 建议值 | 原因 |
|----|--------|------|
| 监听地址 | `127.0.0.1` | 禁止默认公网暴露 |
| Access Token | 强随机串 | 防未授权调用 |
| 网络模式 | WebSocket Server 或反向 WS | 实时推送 |
| 发送行为 | 默认关闭或严格限制 | 降低风控（监控以收为主） |

**部署形态选择**

| 形态 | 优点 | 缺点 |
|------|------|------|
| NapCat Shell / 无头 | 轻量、适合长期挂机 | 与日常 QQ 窗口可能冲突（同号） |
| 带界面 / Framework | 可同时「窥屏」聊天 | 资源占用更高 |
| LLOneBot + 现有 QQ | 不改日常使用习惯 | 依赖 QQ 客户端与插件生态 |

> 同号通常不宜多端同时在线抢登录；落地前需实测本机能否「日常 QQ + NapCat」共存，不能则二选一或改用小号。

### 5.2 监控小服务

**建议技术栈（默认）**

- 语言：Python 3.11+
- WS 客户端：`websockets` / `aiohttp`
- 配置：`pydantic-settings` + `.env`
- 存储（可选一期）：SQLite
- 进程：本机常驻（可后续做成 Windows 服务 / NSSM / 任务计划）

**模块划分**

```text
qq-group-monitor/
├── README.md
├── docs/
│   └── 方案大纲.md          # 可同步本文档
├── .env.example
├── config.yaml.example
├── requirements.txt
├── app/
│   ├── main.py              # 入口：连 WS、主循环
│   ├── config.py            # 群号、Token、端口
│   ├── onebot_client.py     # OneBot 连接与重连
│   ├── filters.py           # group_id / 关键词过滤
│   ├── handlers/
│   │   ├── log_handler.py   # 写日志
│   │   ├── store_handler.py # 可选落库
│   │   └── alert_handler.py # 可选告警
│   └── models.py            # 事件模型
└── data/                    # 日志与 SQLite（gitignore）
```

**核心处理逻辑（伪代码）**

```python
async def on_event(event: dict):
    if event.get("post_type") != "message":
        return
    if event.get("message_type") != "group":
        return
    if str(event.get("group_id")) not in ALLOWED_GROUPS:
        return
    await dispatch(event)  # log / store / alert
```

### 5.3 配置项草案

```yaml
onebot:
  ws_url: "ws://127.0.0.1:3001"
  access_token: "CHANGE_ME"

monitor:
  group_ids:
    - "123456789"          # 目标群，字符串更稳妥
  keywords:                # 可选：关键词告警
    - "报警"
    - "紧急"
  log_all: true            # 是否记录该群全部消息

storage:
  enabled: true
  sqlite_path: "./data/messages.db"

alert:
  enabled: false
  webhook_url: ""          # 飞书/钉钉/自建等
```

---

## 6. 安全与风控

### 6.1 必须遵守

1. OneBot **仅绑定 `127.0.0.1`**
2. 必须配置 **Access Token**，服务端与客户端一致
3. 不把 WebUI / WS 端口映射到公网
4. Token、群号等不进 Git；使用 `.env` + `.gitignore`
5. 监控账号优先**专用小号**；若必须用主力号：少发消息、少调主动 API

### 6.2 风险说明（需知情接受）

| 风险 | 说明 | 缓解 |
|------|------|------|
| 账号风控 | 非官方协议端存在限制登录、封禁可能 | 小号、只收不发、控制频率 |
| 协议变更 | QQ / NapCat 升级可能导致短暂不可用 | 锁定版本、关注更新、留降级方案 |
| 凭证泄露 | Token 泄露可被滥用发消息 | 本机绑定 + 强 Token + 定期轮换 |
| 同号冲突 | 日常 QQ 与协议端抢登录 | 实测兼容性，不行则分号或单开 |

### 6.3 合规提示

本方案用于**本人账号可见范围内**的消息处理（如个人工作辅助、已授权的运营监控）。请确保符合群规、公司规定及适用法律；勿用于未授权窃听或骚扰。

---

## 7. 实施阶段

### 阶段 0：环境确认（0.5 天）

- [ ] 确认监控用 QQ 号已在目标群
- [ ] 确认本机 QQ 版本与 NapCat 兼容说明
- [ ] 决定：NapCat 独立登录 vs LLOneBot 挂接现有客户端
- [ ] 记下目标群 `group_id`（可从 NapCat/调试事件中获取）

### 阶段 1：协议端打通（0.5～1 天）

- [ ] 安装并启动 NapCat
- [ ] 登录账号，WebUI 配置 WS + Token + `127.0.0.1`
- [ ] 用最小脚本打印全部事件，确认能收到群消息

### 阶段 2：监控 MVP（1 天）

- [ ] 实现 WS 连接、断线重连
- [ ] 实现 `group_id` 白名单过滤
- [ ] 控制台 + 文件日志输出
- [ ] `.env.example` 与基本 README

### 阶段 3：增强（按需）

- [ ] SQLite 落库（时间、群、发送者、内容、message_id）
- [ ] 关键词告警 → Webhook
- [ ] Windows 开机自启 / 崩溃拉起
- [ ] 简单健康检查（WS 是否在线）

### 阶段 4：运维固化

- [ ] 版本锁定与升级 checklist
- [ ] 故障排查手册（登出、收不到消息、Token 错误）
- [ ] 备份配置（不含明文 Token 的模板）

---

## 8. 验收清单

| 场景 | 预期 |
|------|------|
| 目标群有人发文本 | 服务在数秒内打出日志 |
| 其他群有人发言 | 服务忽略（无日志或仅 debug） |
| 错误 Token 连接 | 拒绝 / 连不上 |
| NapCat 重启 | 小服务自动重连并恢复接收 |
| 图片/富媒体消息 | 至少记录类型与摘要；完整下载可二期 |

---

## 9. 技术债与后续可选

- 消息去重（`message_id`）防止重连重复消费  
- 多媒体落盘（图片、文件）  
- 多群监控与分群策略  
- 管理面板（仅本机）查看最近 N 条  
- 与飞书/邮件/短信告警链路对接  
- 若日后允许机器人进群，可迁移到官方 Bot，业务层尽量只依赖「标准化消息事件」以降低改造成本  

---

## 10. 决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| 主路径 | NapCat + OneBot + 自研小服务 | 不进群加号、实时性好、生态成熟 |
| 传输 | WebSocket | 推送实时、实现简单 |
| 业务语言 | Python（可调整） | 脚本快、易维护 |
| 存储 | 先日志，再 SQLite | 先打通再增强 |
| 官方机器人 | 本期不做 | 不能拉机器人进群 |

---

## 11. 参考资料

- NapCatQQ 文档：https://napneko.github.io  
- NapCatQQ 仓库：https://github.com/NapNeko/NapCatQQ  
- OneBot 11 理解：社区机器人事件/API 标准（HTTP / WebSocket 上报与调用）  
- 安全警示：OneBot 服务勿裸奔公网，务必 Token + 本机绑定  

---

## 12. 落地说明（已完成）

已按默认选型实现 Python 监控小服务，详见仓库根目录 `README.md`。

| 项 | 落地选择 |
|----|----------|
| 协议端 | NapCat 独立（需本机另行安装登录） |
| 小服务 | Python 3.11+（`python -m app.main`） |
| 一期范围 | 日志 + SQLite 落库 + 可选关键词 Webhook |

**你这边还需完成**

1. 安装并启动 NapCat，个人号登录且已在目标群  
2. 配置正向 WS：`127.0.0.1:3001` + Access Token  
3. 复制 `.env.example` → `.env`，填入 Token 与 `MONITOR_GROUP_IDS`  
4. 启动：`python -m app.main`，在目标群发消息验收  

无 NapCat 时可先跑：`scripts/mock_onebot_ws.py` 做本地联调。

---

*文档结束*
